// Sample data generators removed - app now relies entirely on real API data
// This file is kept for reference but all functions are disabled

export function generateSampleWeightData(days: number = 90) {
  console.warn('Sample data generators have been disabled. Use real API data only.');
  return [];
}

export function generateSampleActivityData(days: number = 90) {
  console.warn('Sample data generators have been disabled. Use real API data only.');
  return [];
}